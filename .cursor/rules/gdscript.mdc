---
description: Правила для GDScript геймплея
globs: ["scripts/**/*.gd"]
---
Домен: индекс терминов в начале docs/PHYSICAL-LANGUAGE.md — целиком не читать.
Spawn: SDF + physics raycast + settle (bootstrap.gd). Lunar gravity 1.62 — не gravity_scale.
Верификация: логика ядра — ./tests/run_one.sh test_*; геймплей/HUD — в запущенной игре (AGENTS.md).
Минимальный diff, без over-engineering. VFX: docs/cheatsheets/vfx-authoring.md, без скриптов в scenes/vfx/.
