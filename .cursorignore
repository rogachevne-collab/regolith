# Scratch: cloned repos, screenshots, logs — not source
.tmp/

# Godot cache (rebuilt on import)
.godot/

# Local editor / platform bundles
bin/
*.zip

# Ropes! — чужой репозиторий (Y:\ropes), сюда приходит junction'ом.
# API шва: docs/cheatsheets/ropes-seam.md
addons/ropes/
