# Verification — `KRN-*` (§3.4, `docs/BUG-HUNT-RC-2026-07-25.md`)

Static-analysis findings re-checked against current tree (uncommitted session changes included).
No fixes applied in this pass. Evidence is file:line static analysis; `test_snapshot_replica.gd`
cited where it already exercises related behaviour. No new headless tests were added.

## Table

| ID | Verdict | Evidence | Sev |
|---|---|---|---|
| **KRN-01** `SW-ORPHAN-REDIRECT` | CONFIRMED | `topology_mutation_service.gd:65-66` erases a fully-dismantled assembly from `world._assemblies` with no scan/prune of `world._redirects` entries pointing at it. `simulation_world.gd:205-206,1144-1145` (`list_redirect_from_ids`/`get_redirect_target_raw`) read `_redirects` verbatim, no existence check. `simulation_snapshot.gd:498-511` then rejects (returns `false`) any snapshot whose redirect chain terminates outside `active_assembly_ids` — i.e. `capture()` never fails, but a later `restore_snapshot`/`create_from_snapshot` on that exact payload does. | keep S1 |
| **KRN-02** `SW-RESTORE-NO-GEN` | CONFIRMED | `simulation_world.gd:1419-1455` (`restore_snapshot`) clears `_body_group_compile_cache`/`_archetype_validation_cache`/`_occupancy_index_cache` but never increments `topology_generation` (only `_notify_topology_changed`, `simulation_world.gd:1897-1898`, does that, and it isn't called). `world_command_gateway.gd:2221-2224` (`snap_context_revision`) returns `topology_generation` directly, consumed by `construction_preview.gd:259-281` as the first field of the preview cache key — a same-aim/archetype preview after a host `restore_snapshot` keys identically to the pre-restore preview. | keep S1 |
| **KRN-03** `SW-BATCH-EVENT-GAP` | CONFIRMED | `simulation_world.gd:2054-2059` (`_emit_structural_event`) drops every event except `world_restored` while `_structural_batch_depth > 0`; `end_structural_batch` (`simulation_world.gd:2037-2051`) only ever emits `structural_batch_committed`. Neither `simulation_physics_projection.gd:316-351`, `element_visual_projection.gd:104-148`, nor `industry_simulation.gd:212-219` has a `structural_batch_committed` case — only `coop_session.gd:1532`+ (dirty-mark for broadcast) reacts to it. `RoverComposer.spawn_on_terrain` (`rover_composer.gd:137-142`) papers over this by manually calling `session.projection.project_assembly_now(...)` / `session.visuals.rebuild_assembly(...)` right after `compose()` returns — confirming the gap is real and is currently only masked by every in-game compose caller going through `spawn_on_terrain`, not by the kernel itself. | keep S1 |
| **KRN-04** `SW-C1-UNGATED` | CONFIRMED | None of `set_resource_amount` (`simulation_world.gd:1122-1142`), `ensure_resource_store` (`1109-1120`), `add_world_loot_pile` (`1038-1044`, delegates to `WorldLootServiceScript` with no gate), or `sync_actuator_observation` (`983-1000`) call `_refuse_replica_write`, unlike the sibling `apply_*` methods a few lines away (`tick_actuators:1002-1003`, `apply_enqueue_recipe:1016-1018`) which do. `begin_structural_batch`/`end_structural_batch` (`2033-2051`) are likewise ungated. Confirmed asymmetry vs. `apply_structural_command_now` (`1201`), which does check. | keep S1 |
| **KRN-05** `SW-RESTORE-BATCH-STALE` | CONFIRMED | `restore_snapshot` (`simulation_world.gd:1419-1455`) never touches `_structural_batch_depth` or `_deferred_derived_recompute`. If restore runs while a batch is open (depth > 0), depth stays > 0 forever after restore since nothing else decrements it outside a matching `end_structural_batch()` call, and `_emit_structural_event` (`2054-2059`) will suppress every future structural event for the rest of the session. Narrow trigger window (needs restore nested inside an open batch), but the code path itself is exactly as described. | keep S2 |
| **KRN-06** `SW-MERGE-LOCO-LEAK` | CONFIRMED | `merge_assemblies` (`topology_mutation_service.gd:272-461`) tombstones the loser (`434-437`) and reassigns joints/elements to the survivor, but never calls `world.clear_assembly_locomotion(loser.assembly_id)` — that call only exists in `remove_element_from_topology`'s full-removal branch (`topology_mutation_service.gd:67`, gated on `remaining_elements.is_empty()`). `list_locomotion_rows` (referenced in the finding) has no tombstone filter of its own to compensate. | keep S2 |
| **KRN-07** `SW-CMD-RAW-ID` | CONFIRMED | `place_element` else-branch (`construction_command_service.gd:222`) resolves `command.assembly_id` via `get_assembly_raw`, not `get_assembly` (which resolves redirects, `simulation_world.gd:1147-1151`). After a merge the loser stays in `_assemblies` as a tombstoned object (never erased), so a client-cached loser id hits `assembly.tombstoned == true` → `REASON_INVALID_REFERENCE`, instead of resolving to the survivor. `dismantle_element` (`1388-1396`) is unaffected because it keys off `element.assembly_id`, which merge keeps current (`topology_mutation_service.gd:415-420`). | keep S2 |
| **KRN-08** `SW-LETHAL-NO-TOPO-REV` | CONFIRMED | `damage_element` (`construction_command_service.gd:1280-1312`) validates only `expected_state_revision` via `world._validate_state_command`; on lethal integrity it calls `world._remove_element_from_topology(...)` (a topology mutation: element erase, possible assembly split) with **no** `expected_assembly_revision` check at all. Contrast `dismantle_element`/`break_rigid_joint` (`topology_mutation_service.gd:174-181`), which gate on `assembly.topology_revision != command.expected_assembly_revision` before mutating. Real asymmetry, no optimistic-concurrency gate on the lethal-damage topology path. | keep S2 |
| **KRN-09** `SW-REV-ASYMMETRY` | CONFIRMED | `industry_network_commands.gd:36-42,51-62` (`connect_network`) only checks revisions when `expected_a >= 0` / `expected_revision_b >= 0` — the command's default `-1` skips the check entirely. `construction_command_service.gd:232-238` (`place_element`) uses a bare `assembly.topology_revision != command.expected_assembly_revision`, so a default `-1` against any live assembly (revision starts at 0/1, never `-1`) deterministically fails with `stale_revision`. Confirmed differing contracts for the same conceptual guard. | keep S2 |
| **KRN-10** `SW-ERASE-VS-TOMBSTONE` | CONFIRMED | Same evidence as KRN-01: `topology_mutation_service.gd:66` erases outright (no tombstone, no redirect cleanup) while `merge_assemblies` (`435-437`) tombstones + redirects and never prunes. Two different lifetime policies for "assembly is gone" (`erase` vs `tombstone+redirect`) coexist with nothing that ever prunes a stale redirect target — this is the structural root cause dedup'd with KRN-01, not a separate bug, but the policy gap itself is real and independently confirmed. | keep S2 |
| **KRN-11** `SW-INDUSTRY-BATCH-STALE` | FALSE ALARM | `IndustrySimulation._tick_once` (`industry_simulation.gd:165-166`) starts every tick by reassigning `_cargo_graph = world.ensure_cargo_graph_current()`, and that helper (`simulation_world.gd:492-495` + `cargo_graph.gd:101-107`) rebuilds off **per-assembly `topology_revision`**, which every place/dismantle bumps immediately (`assembly.bump_revision()` is never itself deferred by batch depth — only `_notify_topology_changed`'s derived-cache clear is, per `simulation_world.gd:1933-1937`). So the runner's handle self-heals on every access, not "until the next recognized event" as the finding claims. Additionally, `RoverComposer`/`MachineComposer.compose()` run `begin_structural_batch()`…`end_structural_batch()` fully synchronously with no `await`, so there is no code path for `_tick_once` (frame/timer-driven) to interleave *inside* an open batch. No live-reproducible staleness window found. | down (drop / no action) |
| **KRN-12** `SW-LOCO-READ-WRITE` | CONFIRMED | `get_locomotion_controller` (`simulation_world.gd:739-744`) lazily inserts a fresh `AssemblyLocomotionController` into `_assembly_locomotion` on read with zero `_refuse_replica_write` gate — any replica-side read (HUD, projection) mutates world state. `test_snapshot_replica.gd:284-288` explicitly works around this ("projection lazily inserts default locomotion rows... baseline is taken after the rebuild on purpose"), i.e. the test authors already knew about and designed around this exact behavior rather than fixing it. | keep S2 |
| **KRN-13** `SW-CONNECT-NO-TOPO-BUMP` | CONFIRMED | `connect_network` (`industry_network_commands.gd:98`) and `disconnect_network` (`286`) each call only `world._industry_network.bump_revision()`; neither calls `_notify_topology_changed()`/bumps `topology_generation`. Any cache keyed solely on `topology_generation` (e.g. `construction_preview`'s `snap_context_revision`, see KRN-02) is blind to wire-only mutations. | keep S3 |
| **KRN-14** `SW-BATCH-PARTIAL-WORLD` | CONFIRMED | `MachineComposer.compose` (`machine_composer.gd:20-23`) and `RoverComposer.compose` (`rover_composer.gd:21-24`) both call `world.end_structural_batch()` unconditionally after `_compose_batched(...)`, including the `validate_failed` return path (`machine_composer.gd:49-58`, `rover_composer.gd:69-78`) and any earlier `helper.place(...)`/`_place_*` failure. `begin_structural_batch`/`end_structural_batch` (`simulation_world.gd:2033-2051`) have no snapshot/rollback machinery — whatever elements were placed before the failure stay in the world, and `end_structural_batch` still fires `structural_batch_committed` for that half-built assembly. | keep S3 |
| **KRN-15** `SW-RECONCILE-DOUBLE-BUMP` | CONFIRMED | `reconcile_terrain_anchors_for_assemblies` (`construction_command_service.gd:1508-1510`) calls `assembly.bump_revision()` + `world._notify_topology_changed(assembly.assembly_id)` when anchor joints changed, and callers (`topology_mutation_service.gd:88-90`, `205-207`, `252-254`) invoke this *before* their own explicit `assembly.bump_revision()` + `_notify_topology_changed` a couple lines later. When anchors actually change as a side effect of the same op, one logical structural command produces two revision bumps and two topology-changed notifications. Cosmetic/diagnostic (both bumps are individually correct, no data loss), matches finding's own S3. | keep S3 |

## Counts

| Verdict | Count | IDs |
|---|---|---|
| CONFIRMED | 14 | KRN-01, KRN-02, KRN-03, KRN-04, KRN-05, KRN-06, KRN-07, KRN-08, KRN-09, KRN-10, KRN-12, KRN-13, KRN-14, KRN-15 |
| FALSE ALARM | 1 | KRN-11 |
| PARTIAL | 0 | — |
| NEEDS_PLAYTEST | 0 | — |
| **Total** | **15** | KRN-01…KRN-15 |

Severity changes vs. catalog: **KRN-11 downgraded from S2 to dropped** (false alarm — the described
staleness window is not reachable given synchronous, non-yielding compose batches and the
self-healing per-assembly-revision check in `ensure_cargo_graph_current`). All other 14 entries keep
their cataloged severity; none needed a bump.

No `KRN-*` entry required a live playtest — all are pure kernel logic (topology/snapshot/replica
gating/batch events), reachable and falsifiable by reading `scripts/simulation/**` alone, consistent
with the doc's own R2 guidance that kernel logic is headless-testable. None were run as a headless
probe in this pass (static analysis was sufficient to confirm/refute each); a follow-up fix batch
(per `docs/BUG-HUNT-RC-2026-07-25.md` §5 batches B3/B4/B5/B17) should add regression tests for
KRN-01, KRN-02, KRN-06, KRN-07, KRN-08, KRN-09 before patching, since none of the existing
`test_snapshot_*`/`test_simulation_runtime.gd` suites cover the orphan-redirect, no-gen-bump,
merge-loco-leak, raw-id-post-merge, lethal-no-topo-rev, or revision-asymmetry scenarios today.
